{
  "name": "sws-production-calendar",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:pages": "node scripts/build-pages.mjs",
    "check": "node --check Code.js && node --check js/app.js && node --test tests/*.test.js tests/*.test.mjs",
    "test": "node --test tests/*.test.js tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}

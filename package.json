{
  "name": "sws-production-calendar",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:backend": "node scripts/build-backend.mjs",
    "build:pages": "node scripts/build-pages.mjs",
    "check": "npm run build:backend && npm run check:syntax && npm run test:coverage",
    "check:syntax": "node scripts/check-syntax.mjs",
    "test": "npm run build:backend && node --test tests/*.test.js tests/*.test.mjs",
    "test:coverage": "node --experimental-test-coverage --test-coverage-lines=80 --test tests/*.test.js tests/*.test.mjs",
    "test:e2e": "playwright test",
    "test:load": "node scripts/load-simulation.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "pdfjs-dist": "^4.7.76"
  },
  "engines": {
    "node": ">=22"
  }
}
